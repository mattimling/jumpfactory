function sliderGallery() {

    var swiper = new Swiper(".js-swip", {
        slidesPerView: 'auto',
        freeMode: true,
        grabCursor: true,
    });

}

// Initialize the dragGallery function
sliderGallery();
